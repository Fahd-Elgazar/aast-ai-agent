# Web interface

React and TypeScript interface for academic advising, graph visualization, conversation history, and program recommendations.

## Commands

```powershell
npm ci
npm run lint
npm run build
npm run dev
```

Copy `.env.example` to `.env`. In Docker, `VITE_API_BASE=/api` is routed to the advisor API by Nginx.
