# Security

Do not commit API keys, passwords, local `.env` files, databases, model caches, or student data.

Use long, unique values for `INTERNAL_SECRET_KEY` and `NEO4J_PASSWORD`. Rotate any credential that appears in Git history or logs.

If you discover a security issue, report it privately to the repository owner instead of opening a public issue.
